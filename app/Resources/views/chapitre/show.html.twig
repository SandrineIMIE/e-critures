{% extends 'base.html.twig' %}

{% block body %}
    <table>
        <tbody>
        <tr>
            <th><h1>{{ chapitre.title }}</h1></th>
            <td>
                <a href="{{ path('chapitre_edit', { 'id': chapitre.id }) }}" class="btn btn-info">Modifier</a>&nbsp;
            </td><td> {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer"  class="btn btn-danger">
                {{ form_end(delete_form) }}
            </td>
        </tr>

        <tr>

        </tr>
        </tbody>
    </table>
{% endblock %}