{% extends 'admin.html.twig' %}
    {% block dahsbord%}

    {% endblock %}

{% block body %}
    <h1>Liste des droits</h1>
<ul>
        {% for right in rights %}
            <li>{{ right.type }}
               <a href="{{ path('rights_edit', { 'id': right.id }) }}" class="btn btn-info" role="button" title=" Modifier">
                   <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
            </li>
        {% endfor %}
</ul>


{% endblock %}
