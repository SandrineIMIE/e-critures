<ul id="menu-accordeon">
    <li><a href="#"><span class="glyphicon glyphicon-folder-open" aria-hidden="true"></span> Chapitre(s)</a>
        <ul id="listchap">
            {% for chapitre in chapitres %}
            <li>
                <a href="{{ path('chapitre_edit', { 'id': chapitre.id }) }}">{{ chapitre.title }}</a>
            </li>
            {% endfor %}
            <li>
                <a href="{{ path('chapitre_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#" id="linkshow"><span class="glyphicon glyphicon-link" aria-hidden="true"> </span> Liens</a>
        <ul id="listliens">
            {% for link in links %}
                <li>
                    <a href="{{ link.adress }}" target="_blank"> {{ link.name }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="{{ path('link_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-pushpin" aria-hidden="true"> </span> Notes</a>
        <ul>
            <li id="listnotes">
                note
            </li>
            <li>
                <a href="{{ path('note_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-map-marker" aria-hidden="true"></span> Lieux </a>
        <ul id="listlieux">

            <li>
                <a href="{{ path('place_list', { 'id': project.id }) }}">Liste des Lieux</a>
            </li>
            <li>
                <a href="{{ path('place_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Personnages</a>
        <ul id="listperso">
            <li>
                <a href="{{ path('heros_list', { 'id': project.id }) }}">Liste des personnages</a>
            </li>
            <li>
                <a href="{{ path('heros_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span> Lexique</a>
        <ul id="listlexique">
            <li>
                <a href="{{ path('lexicom_list', { 'id': project.id }) }}">Lexique</a>
            </li>
            <li>
                <a href="{{ path('lexicom_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-wrench" aria-hidden="true"></span> Objets</a>
        <ul id="listobjet">
            {% for item in items %}
            <li>
                <a href="{{ path('items_show', { 'id': item.id }) }}">{{ item.name }}</a>
            </li>
            {% endfor %}
            <li>
                <a href="{{ path('items_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-calendar" aria-hidden="true"></span> Evenements</a>
        <ul id="listevents">
            {#{% for event in events %}#}
            {#<li>#}
                {#<a href="{{ path('events_list', { 'id': project.id }) }}">Liste des Evenements</a>#}
            {#</li>#}
            {#{% endfor %}#}
            <li>
                <a href="{{ path('events_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-tags" aria-hidden="true"></span> Tags</a>
        <ul id="listtags">
            <li>
                Liste des tags
                {#<a href="{{ path('tag_list', { 'id': project.id }) }}">Liste des tags </a>#}
            </li>
            <li>
                <a href="{{ path('tag_new', { 'id': project.id }) }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
</ul>
