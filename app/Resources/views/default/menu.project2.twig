{#<form class="form-inline" role="search">#}
    {#<div class="form-group">#}
        {#<div class="input-group">#}
            {#<div class="input-group-addon"><span class="glyphicon glyphicon-search" aria-hidden="true"></span></div>#}
            {#<label class="sr-only" for="SearchProjec">Recherche dans le Projet</label>#}
        {#<input type="text" class="form-control" name="SearchProject" placeholder="Recherche dans le projet">#}
        {#</div>#}
    {#<button type="submit" class="btn btn-primary"> go</button>    </div>#}
{#</form>#}
<div class="clearfix"></div>
<ul id="menu-accordeon">
    <li><a href="{{ path('project_list', {'id':project.getUser.id }) }}" title="Retour à mes Projets">
            <span class="glyphicon glyphicon-book" aria-hidden="true"></span>&nbsp;Retour à mes Projets</a>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-folder-open" aria-hidden="true"></span>&nbsp; Chapitre(s)</a>
        <ul id="listchap">
            {% for chapitre in chapitres %}
                <li>
                    <a href="{{ path('chapitre_edit', { 'id': chapitre.id }) }}" title="{{ chapitre.title }}">{{ chapitre.title }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="{{ path('chapitre_new',{ 'id': project.id }) }}" title="Ajouter un Chapitre">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#" id="linkshow"><span class="glyphicon glyphicon-link" aria-hidden="true"> </span>&nbsp;Liens</a>
        <ul id="listliens">
            {% for link in links %}
                <li>
                    <a href="{{ link.adress }}" target="_blank" title="{{ link.name }}"> {{ link.name }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('link_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false;"title="Ajouter un Lien">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true" ></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-pushpin" aria-hidden="true"> </span>&nbsp;Notes</a>
        <ul id="listnotes">
            {% for note in notes %}
                <li>
                    <a href="{{ path('note_show', { 'id': note.id }) }}" title="{{ note.titre }}">{{ note.titre }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('note_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false;" title="Ajouter une note">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true" ></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>&nbsp;Objets</a>
        <ul id="listobjet">
            {% for item in items %}
                <li>
                    <a href="{{ path('items_show', { 'id': item.id }) }}" title="{{ item.name }}">{{ item.name }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('items_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false;" title="Ajouter un objet">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-calendar" aria-hidden="true"></span>&nbsp;Evenements</a>
        <ul id="listevents">
            {% for event in events %}
                <li>
                    <a href="{{ path('events_show', { 'id': event.id }) }}" title="{{ event.date }}">{{ event.date }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('events_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false;" title="Ajouter un événement">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-map-marker" aria-hidden="true"></span> Lieux </a>
        <ul id="listlieux">
            {% for place in places %}
                <li>
                    <a href="{{ path('place_show', { 'id': place.id }) }}" title="{{ place.name }}">{{ place.name }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('place_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false;" title="Ajouter un lieu">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>

    <li><a href="#"><span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span> Lexique</a>
        <ul id="listlexique">
            {% for lexicom in lexicoms %}
                <li>
                    <a href="{{ path('lexicom_show', { 'id': lexicom.id }) }}" title="{{ lexicom.mot }}">{{ lexicom.mot }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('lexicom_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false;" title="Ajouter un mot dans le lexique">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Personnages</a>
        <ul id="listperso">
            {% for perso in persos %}
                <li>
                    <a href="{{ path('perso_show', { 'id': perso.id }) }}" title="{{ perso.prenom }}&nbsp;{{ perso.nom }}">{{ perso.prenom }}&nbsp;{{ perso.nom }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('perso_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false;" title="Ajouter un personnage">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
    <li><a href="#"><span class="glyphicon glyphicon-tags" aria-hidden="true"></span> Tags</a>
        <ul id="listtags">
            {% for tag in project.tags %}
                <li>
                    <a href="{{ path('tag_show', { 'id': tag.id }) }}" title="{{ tag.name }}"> {{ tag.name }}</a>
                </li>
            {% endfor %}
            <li>
                <a href="#"  onclick="window.open('{{ path('tag_new', { 'id': project.id }) }}',
                        'Popup_Window','width=600, height=400'); return false; " title="Ajouter un tag">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </li>
</ul>
