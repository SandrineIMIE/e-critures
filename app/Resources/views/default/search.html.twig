<form class="navbar-form navbar-right">
    <label class="sr-only" for="SearchProjec">Trouver dans E-critures</label>
    <input type="text" class="form-control" name="SearchSite" placeholder="Trouver dans E-critures...">
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        <span style="color: white">Welcome <b>{{ app.user.username }}</b></span>
        <a href="{{ path('fos_user_security_logout') }} " class="btn btn-danger">Déconnexion</a>
    {% else %}
        <a href="{{ path('fos_user_security_login') }}" class="btn btn-primary">Connexion</a>

    {% endif %}
</form>