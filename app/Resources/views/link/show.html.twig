{% extends 'popup.html.twig' %}

{% block body %}
    <table>
        <tbody>
            <tr>
                <th>Adresse</th>
                <td><a href="{{ link.adress }}"> {{ link.adress }}</a> </td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ link.name }}</td>
            </tr>
        <tr>
            <th>Actions</th>
            <td><a href="{{ path('link_edit', { 'id': link.id }) }}" class="btn btn-info"
                   role="button" title="Modifier">  <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                {{ form_start(delete_form) }}
                <button type="submit" value="Supprimer" class="btn btn-danger" title="Supprimer">
                    <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                </button>
                {{ form_end(delete_form) }}</td>
        </tr>
        </tbody>
    </table>

{% endblock %}
