{% extends 'base.html.twig' %}

{% block body %}
    <h1>Link</h1>

    <table>
        <tbody>

            <tr>
                <th>Adress</th>
                <td>{{ link.adress }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ link.name }}</td>
            </tr>
        <tr>
            <th>Actions</th>
            <td><a href="{{ path('link_edit', { 'id': link.id }) }}">Modifier</a>
                {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
                {{ form_end(delete_form) }}</td>
        </tr>
        </tbody>
    </table>

{% endblock %}
