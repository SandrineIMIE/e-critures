{% extends 'popup.html.twig' %}
{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
    <button type="submit" value="Créer" class="btn btn-primary" title="Sauver">
        <span class="glyphicon glyphicon-save" aria-hidden="true"></span>
    </button>
    {{ form_end(form) }}
{% endblock %}
