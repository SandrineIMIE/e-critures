{% extends 'base.html.twig' %}

{% block body %}
    <h1>Utilisateur</h1>

    <table>
        <tbody>

            <tr>
                <th>Nom d'utilisateur</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Mot de passe</th>
                <td>{{ user.password }}</td>
            </tr>
            {#<tr>#}
                {#<th>Salt</th>#}
                {#<td>{{ user.salt }}</td>#}
            {#</tr>#}
            <tr>
                <th>Rôles</th>
                <td>{% if user.roles %}{{ user.roles|join(', ') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        {#<li>#}
            {#<a href="{{ path('user_index') }}">Back to the list</a>#}
        {#</li>#}
        <li>
            <a href="{{ path('user_edit', { 'id': user.id }) }}" class="btn btn-info"
                role="button" title="Modifier">  <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <button type="submit" value="Supprimer" class="btn btn-danger" title="Supprimer">
                <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
            </button>
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
