{% extends 'popup.html.twig' %}

{% block body %}
    <h1>Place creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}


{% endblock %}
